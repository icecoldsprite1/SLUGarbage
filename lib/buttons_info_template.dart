import 'package:flutter/material.dart';

class ButtonInfo extends StatelessWidget {
  const ButtonInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Someone implement this')),
    );
  }
}
